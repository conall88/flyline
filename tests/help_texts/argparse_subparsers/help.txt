usage: myapp [-h] {init,run,stop} ...

My Application

positional arguments:
  {init,run,stop}
    init           Initialize the project
    run            Run the application
    stop           Stop the application

optional arguments:
  -h, --help       show this help message and exit
  --verbose        Enable verbose output
